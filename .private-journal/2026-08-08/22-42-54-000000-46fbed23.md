---
id: 46fbed23-3819-42be-a54e-ba4e39c9393a
date: "2026-08-08T22:42:54-05:00"
type: project
---

## Project Notes

Critical repair batch implemented after the 2026-08-08 audit. Unknown-city 404s
are plain text and the live payload is inert. Docker now copies a locked
`/opt/venv` between identical Python 3.13 stages, binds to the container network
by default, omits development artifacts, and passes a no-cache build plus live
health request. NWS alerts use only `point` and `status`; alert failures and
malformed alert payloads fail closed, while forecast failures or malformed
forecast data preserve valid alerts. PR #24 was closed as superseded.

Verification: 284 tests pass at 83% coverage; Ruff formatting and Bandit pass.
The correct project dependency audit first found 20 advisories across 12
packages. Doctor Biz approved a full lock refresh: 35 packages updated, 3 added,
1 removed, and the audit now reports zero known vulnerabilities. Three new MyPy
diagnostics from the tool refresh were fixed without runtime changes, restoring
the existing backlog of 18; 27 Ruff errors also remain outside this diff. I feel
good about the narrow security and safety boundaries, but I would not ship
publicly before replacing the unsafe Werkzeug production server and addressing
the other critical audit findings.
